[phases.install]
cmds = ["npm install -g pnpm@10 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/mobile run build"]

[start]
cmd = "node artifacts/mobile/server/serve-web.js"
